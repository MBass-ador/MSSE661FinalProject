// imports
/*const express = require('express');

const { 
    getUser,
    updateUser, 
    deleteUser 
} = require('../controllers/user.controller');

const canAccess = require('../middleware/auth.middleware');

// new router
const userRoutes = express.Router();

// routes to retrieve, modify or delete user (by name) 
// (same path, different methods)
userRoutes.get('/:name', canAccess, getUser); // http://localhost:3000/api/user
userRoutes.put('/:name', canAccess, updateUser); // http://localhost:3000/api/user
userRoutes.delete('/:name', canAccess, deleteUser); // http://localhost:3000/api/user

// exporting routes
module.exports = userRoutes;*/